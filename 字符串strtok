#include <stdio.h>
#include <string.h>
int main()
{
char arr[]="192.168.1.157";     //点分十进制strtok
   char *p=arr;
   char* p1=".";
   char* ret;
   for(ret=strtok(p,p1);ret != NULL;ret=strtok(NULL,p1))     //循环体注意
      printf("%c\n");
   return 0;
}
